import express from 'express';import AprendizagemController from '../controllers/AprendizagemController.js';import { autenticado } from '../middlewares/autorizacao.js';import { assinc } from '../middlewares/erros.js';const router=express.Router();const controle=new AprendizagemController();router.get('/aprender',assinc(controle.list));router.get('/aprender/:id',assinc(controle.detalhes));router.post('/aprender/:id/progresso',autenticado,assinc(controle.progresso));export default router;
