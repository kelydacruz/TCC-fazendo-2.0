import express from 'express';import PainelController from '../controllers/PainelController.js';import { autenticado } from '../middlewares/autorizacao.js';import { assinc } from '../middlewares/erros.js';const router=express.Router();const controle=new PainelController();router.get('/painel',autenticado,assinc(controle.index));router.post('/tccs/:id/favorito',autenticado,assinc(controle.favoritoTcc));export default router;
